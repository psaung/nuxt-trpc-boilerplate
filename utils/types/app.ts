export interface loginProps {
  email: string;
  password: string;
  rememberMe?: boolean;
}

export interface registerProps {
  firstName: string;
  lastName: string;
  password: string;
  email: string;
  phone: string;
  address: string;
}
